.DS_Store
.pi/
node_modules/

# Per-repo skill symlinks that project-setup creates when a repo consumes
# engineering/ skills. The source of truth is this repo; the symlinks are not
# committed anywhere. Ignored here in case this repo dogfoods its own skills.
.claude/skills/
.opencode/skills/
