---
name: grill
description: >-
  Scrutinize an idea or one branch of a map until user and agent share
  understanding. Walk the logic tree, resolving decision dependencies one
  numbered question at a time, each with a recommended answer. Use on "grill",
  "pressure-test", "interrogate this". Writes a grill artifact to ~/Dev/notes.
  Depth/convergence pass; pair with map to diverge.
---

# grill

The general front door to convergence: any subject, no preloaded context.

1. **Restate the scope**: a bare idea, a decision, or one node of a map. Given
   a whole map, name the branch you're starting on and why.
2. **Compose `converge`** on that subject. Lens: look nothing up. Every
   load-bearing decision, and every fact you cannot derive from what the user
   has already said, is a question for the user. To grill against a live
   codebase instead, use `codebase-grill`.
3. **Exit.** `converge`'s convergence and artifact apply unchanged.
